services:
    swagger:
      image: swaggerapi/swagger-ui
      ports:
        - "8083:8080"
      volumes:
        - ./api/openapi.yaml:/openapi.yaml:ro
      environment:
        SWAGGER_JSON: /openapi.yaml
