# DailyArt

OpenAPI specification: `openapi.yaml`

## Swagger UI

Локальный запуск через Docker Compose:

```bash
docker compose -f docker-compose.swagger.yml up
```

Swagger UI будет доступен на `http://localhost:8080`.

Остановить:

```bash
docker compose -f docker-compose.swagger.yml down
```
